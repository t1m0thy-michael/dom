const chai = require('chai')
const assert = chai.assert

import { attribute } from './attributes'

describe('attributes', function () {

	it('does stuff', function () {	
		assert.ok(true, 'tests doing test stuff')
	})

})